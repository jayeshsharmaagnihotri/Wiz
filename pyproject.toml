[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wiz"
version = "0.1.0"
dependencies = [
    "questionary"
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["wiz"]

[project.scripts]
wiz = "wiz.cli:main"
